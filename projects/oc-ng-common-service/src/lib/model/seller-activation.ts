export class SellerActivation { 
    password: string;
    email: string;
    code: string;
}

export class SellerResetPassword{
    newPassword: string;
    email: string;
    code: string;
}